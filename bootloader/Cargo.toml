[package]
name = "bootloader"
version = "0.1.0"
authors = ["Tock Project Developers <tock-dev@googlegroups.com>"]

[dependencies]
kernel = { git = "https://github.com/tock/tock", rev = "405417" }
#kernel = { path = "../../tock/kernel" }

tock-bootloader-protocol = { path = "../protocol" }
